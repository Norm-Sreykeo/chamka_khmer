// This file will contain the AddressScreen widget implementation.
import 'package:flutter/material.dart';

class AddressScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Address Screen'),
      ),
    );
  }
}